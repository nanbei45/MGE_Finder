# MGE_Finder

Please use the following command, `snakemake`, and then replace the actual paths in `config.yaml` and `Snakefile`. This will automatically create the environments for Genomad, IntegronFinder, MobileOG, and ISEScan, and finally output the results to the directory you specified.
```Bash
conda install snakemake=5.26.0
```
